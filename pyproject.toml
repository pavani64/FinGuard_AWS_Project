[project]
name = "de-project"
version = "0.1.0"
dependencies = [
    "boto3>=1.42.97",
    "botocore[crt]>=1.43.90",
    "kagglehub>=0.3.13",
    "pandas>=2.3.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fin_guard"]
